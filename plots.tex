\documentclass{standalone}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.15} %

% ============================================================
% STEUERUNGSZENTRALE: Nur hier Werte ändern!
% ============================================================

% 1. Deine Funktionen
\tikzset{ %
    declare function={ %
        f(\x) =0.5 *sin(0.5*pi*\x r);      % Erste Funktion (Parabel)
        g(\x) =0.5 *cos(0.5*pi*\x r);   % Zweite Funktion (Gerade)
    } %
} %

% 2. Sichtbares Raster für das KKS
\def\rasterStartX{0} %
\def\rasterEndX{6.5}   %
\def\rasterStartY{-1.5} %
\def\rasterEndY{1.5}   %

% 3. Text für die Beschriftungen (wird für Legende UND Direkt-Label genutzt)
\def\labelNameA{$f(x)$} %
\def\labelNameB{$g(x)$} %

% ============================================================


\begin{document}

\begin{tikzpicture}
\begin{axis}[ %
    xmin=\rasterStartX, xmax=\rasterEndX, %
    ymin=\rasterStartY, ymax=\rasterEndY, %
    domain=\rasterStartX:\rasterEndX, %
    %
    x=1cm, y=1cm, %
    xtick distance=1, %
    ytick distance=1, %
    minor tick num=1, %
    grid=both, %
    major grid style={line width=.2pt,draw=gray!50}, %
    minor grid style={line width=.1pt,draw=gray!20}, %
    axis lines=middle, %
    %
    xlabel={$x$}, %
    xlabel style={at={(current axis.right of origin)}, anchor=south, xshift=0.0cm, yshift=-0.0cm}, %
    ylabel={$y$}, %
    ylabel style={at={(current axis.above origin)}, anchor=west, xshift=0.0cm, yshift= 0.0cm}, %
    %
    enlargelimits=false, %
    axis line style={->, shorten >=-0.25cm, shorten <=-0.25cm}, %
    %
    % --------------------------------------------------------
    % AUTOMATISCHE LEGENDE: Hier wird sie eingestellt
    % --------------------------------------------------------
    legend pos=north west, % Position: north west (oben links), north east (oben rechts), etc.
    legend cell align={left}, % Text in der Legende linksbündig
    legend style={draw=gray!40, fill=white, font=\small}, % Dezenter Rahmen, weißer Hintergrund
    %
    % Für leere Achsen:
    xticklabel=\empty, %
    yticklabel=\empty, %
    samples=150 %
] %

% ------------------------------------------------------------
% DIE GRAPHEN
% ------------------------------------------------------------

% Graph 1: Funktion f(x) in Blau mit Label oben links am Graphen
\addplot[color=blue, very thick] {f(x)} node[pos=0.4,  left] {$f(x)$}; %

% Graph 2: Funktion g(x) in Rot mit Label unten links am Graphen
\addplot[color=red, very thick] {g(x)} node[pos=0.8, below ] {$g(x)$}; %


% ------------------------------------------------------------
% LEGENDEN-EINTRÄGE (Übernimmt automatisch die Namen von oben)
% ------------------------------------------------------------
% Wenn du KEINE Legenden-Box willst, lösche einfach diese Zeile:
% \legend{\labelNameA, \labelNameB} %

\end{axis} %
\end{tikzpicture}

\end{document}